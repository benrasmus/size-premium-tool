# Both variables are optional. With neither set, the site is open (no login) --
# appropriate for local/personal use. Set SITE_PASSWORD to enable the login gate,
# e.g. before deploying somewhere publicly reachable.
SITE_PASSWORD=change-me
AUTH_SALT=change-me-to-any-random-string
